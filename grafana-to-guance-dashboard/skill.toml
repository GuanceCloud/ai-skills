id = 'grafana-to-guance-dashboard'
publisher = 'pe'
name = 'grafana-to-guance-dashboard'
version = '0.2.1'
description = 'Convert Grafana dashboard JSON into Guance dashboard JSON with a fully self-contained skill package. Use when the user wants to analyze Grafana dashboards before conversion, run conversion, audit conversion gaps, improve units or PromQL compatibility with LLM-assisted review, repair panel or variable mappings, preserve settings, groups, and vars as much as possible, validate generated Guance dashboard JSON against the bundled schemas, or debug why a Grafana dashboard does not convert cleanly.'
category = 'general'
author = 'liurui'
license = 'NOASSERTION'
homepage = ''
repository = ''
tags = ['dashboard', 'grafana', 'guance']
icon = ''
min_codex_version = ''
tools_required = []
permissions = ['browser', 'filesystem-read', 'filesystem-write', 'network', 'secrets', 'shell']
readme_summary = 'Convert Grafana dashboard JSON into Guance dashboard JSON with a fully self-contained skill package. Use when the user wants to analyze Grafana dashboards before conversion, run conversion, audit conversion gaps, improve units or PromQL compatibility with LLM-assisted review, repair panel or variable mappings, preserve settings, groups, and vars as much as possible, validate generated Guance dashboard JSON against the bundled schemas, or debug why a Grafana dashboard does not convert cleanly.'
view = 'community'
